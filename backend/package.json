{
  "name": "backend",
  "version": "1.0.0",
  "description": "在 /backend 中進行 `npm init`、 `npm install express` 後：",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.0"
  }
}
